apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vless.fullname" . }}-test-connection"
  labels:
    {{- include "vless.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vless.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
