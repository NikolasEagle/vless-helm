apiVersion: v1
kind: Service
metadata:
  name: {{ include "vless.fullname" . }}
  labels:
    {{- include "vless.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vless.selectorLabels" . | nindent 4 }}
